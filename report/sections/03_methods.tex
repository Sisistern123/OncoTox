\section{Methods}
+ scGPT erklären wie es funktioniert/trainiert
+ scGPT wieso? hypothese staten
\subsection{Data}
\label{sec:data}

All experiments use the highest-confidence intersection of SCP542 \cite{scp542} and CTRPv2 \cite{ctrpv2,ctrp_rees}. This intersection comprises \NLines{} cell lines with
post-quality-control measurements (190 roster name matches, of which 9 are unscreened), \NDrugsModelled{}
compounds and \NCells{} cells, with effectively complete viability coverage inside the overlap. That
compound count is smaller than CTRPv2's catalogue of \NDrugs{}: a compound enters the target matrix
only if it was screened against at least fifty of the overlapping cell lines, and eleven fall below
that. The requirement exists to keep output heads with very little support out of the shared trunk;
the threshold itself was set rather than derived, is not taken from any published convention, and has
not been varied, so it should be read as an arbitrary cutoff whose effect on the results is unmeasured.
Genes are
reduced to the \NHvg{} most highly variable; scGPT embeds the \NVocab{} of these that lie in its
vocabulary, while PCA is computed on all \NHvg{}.
\begin{revisionblock}
\textbf{That vocabulary count is not what it appears --- found 05.08.2026.} The embedding script
resolves gene symbols against scGPT's vocabulary by exact string match, and the expression matrix
carries an older HGNC annotation than the vocabulary does. Genes renamed between the two releases
therefore fail the match and are discarded although the vocabulary holds them under their current
name. Recovering renames from the HGNC approved-symbol set shows that \NRescuedHvg{} of the
\NHvg-gene variant's discarded genes fall into this class, and \NRescuedAll{} of the
full-transcriptome variant's --- the latter carrying \PctRescuedAll\% of every cell's transcriptome,
among them RACK1, the ATP-synthase subunit ATP5F1E and the replication-dependent histones, each
expressed in over 99\% of cells \cite{hgnc}. Only about a fifth of a percent of the discarded
expression is genuine vocabulary coverage, so the gene-count asymmetry between the two
representations is largely a defect rather than, as stated elsewhere in this report, a property of
the model being compared.

The consequence exceeds the missing genes themselves. scGPT bins expression using quantiles of each
cell's own non-zero values, so removing the most strongly expressed genes shifts every bin boundary
and alters the bin assigned to every remaining gene. The defect is confined to the scGPT arm ---
principal components and the variability ranking are both computed before the discard --- and it
therefore weakens the representation this work argues for. Every scGPT-versus-PCA margin reported
here is conservative with respect to it, never inflated. The repair is implemented in the pipeline:
the conversion step now records each row's current symbol alongside the identifier the dataset
distributes, and the embedding step consults it only where a row's own symbol is not in the
vocabulary, so no gene embedded today can be lost by it. It raises the \NHvg-gene variant from
\NVocabDirect{} genes to \NVocab{}. \textbf{No representation has been regenerated}, deliberately:
the embeddings are rebuilt once, from a settled pipeline, at the end of the review, so every number
in this report belongs to the pipeline as described before this paragraph.

\revision{\textbf{Superseded 14.08.2026.} The sentence above was true when written and is not now:
the rebuild happened on 12.--13.08.2026 and all five gene-set variants were re-embedded. The
counts in this paragraph are the measured post-rebuild values rather than the predictions they
were; the predicted \NHvg{} figure had been 4{,}704 against a measured 4{,}765.}
\end{revisionblock}
The \NHvg-gene selection is used throughout.
\revision{\emph{The justification for that choice is withdrawn (12.08.2026).} This sentence read ``the
gene-set size is not critical'', on the evidence of an earlier sweep from 1{,}000 genes to the full
transcriptome that showed no advantage for any particular count. That sweep has no live numbers: it was
computed on the legacy \code{mean\_pv} response score, which has since been removed from the pipeline
along with its reader, and the cache it was read from is archived. It was cited as \code{notebooks/07},
a path that no longer exists; the sweep now lives in
\code{notebooks/analysis/qc/verify\_variants.ipynb}, \S9, and has not been re-run. So
\NHvg{} is at present a choice carried forward from a superseded measurement, not one this report can
justify --- and the two readings below narrow what the measurement could ever have supported.}
\revision{\textbf{Restored 14.08.2026.} The sweep has been re-run. All five gene-set variants were
re-embedded on 13.08.2026, so its points now differ in gene count alone rather than in gene count
\emph{and} code version, and it is like-for-like for the first time. Over the whole range from
1{,}000 genes to the full transcriptome the principal-component arm moves 5.8 heads-beating-baseline
out of \NDrugsModelled{}, against a fold-to-fold standard deviation of 27.2 --- flat within error for both
representations. The withdrawn sentence is therefore supported again on current numbers, and \NHvg{}
rests on that measurement rather than on inheritance. The correction immediately below still applies:
what the sweep compares for the transcript-level arm is gene \emph{selection} at a fixed per-cell cap,
not gene count.}
\revision{\emph{Correction, 05.08.2026.} That sweep's \emph{all-genes} arm is also not the full transcriptome as far as
scGPT is concerned: the model reads at most 1{,}200 genes per cell, drawn at random from that cell's
non-zero genes, and in the all-genes variant every cell exceeds that cap. What the sweep compared for
scGPT was therefore about 589 dispersion-selected genes per cell at \NHvg{} against about 1{,}199
randomly drawn ones at all-genes --- roughly twice as many genes, but chosen at random rather than
selected. The flat result supports only the narrow statement that random selection at twice the count
does no better than dispersion selection; it is not evidence that scGPT gains nothing from the full
transcriptome, which the model never received. The comparison is unaffected for the principal-component
representation, which reads every gene it is given. Both readings are in any case untested on the
current target.}

\begin{revisionblock}
\textbf{Quality control inherited from the source (added 05.08.2026).} SCP542 does not arrive raw, and
this project applies no cell-level filtering of its own, so the figures above are the output of the
source study's quality control rather than a raw measurement \cite{scp542}. Cells were profiled in nine
multiplexed pools on the 10x Chromium platform, at an average of about 280 cells per line; barcode
filtering, alignment and molecule counting used Cell Ranger 3.0.1. Cells were assigned to cell lines by
the consensus of a genetic and an expression-based method, with inconsistent assignments --- largely
cells of low sequencing coverage --- discarded. The number of genes with a non-zero measurement in a
given cell, its detected-gene count, served as the quality proxy, and cells were retained when that
count fell between 2{,}000 and 9{,}000. The two bounds catch different failures: a count below the
floor indicates that almost no RNA was captured, as in an empty droplet carrying only ambient
transcript or a cell whose contents have leaked out, while a count above the ceiling indicates that the
droplet held two cells rather than one, since two transcriptomes together present more distinct genes
than either alone. The window therefore retains droplets consistent with a single intact cell.
Suspected doublets and low-quality cells were removed on this basis, as were cell lines with fewer than
fifty assigned cells. What
remains is the \NCells{} cells and 198 cell lines used here, at an average of 19{,}264 unique molecular
identifiers and 3{,}802 detected genes per cell. The gene filters the source study applied for its own
heterogeneity analyses were \emph{not} applied to the distributed matrix, which retains \NGenes{}
genes, so the gene selection described below is independent of them.

No further quality control is performed here, and none is warranted. The doublet call in particular
rests on information the distributed matrix does not carry: because cells were assigned to lines by
genotype, a droplet containing two different cell lines presents mixed variants and is identified
directly, which is a stronger discriminator than expression-based inference and cannot be reconstructed
from expression alone. Applying a second, generic filtering pass would remove genuine cells at the
margins of an already filtered population without recovering that signal. Two consequences of the
inherited filter are worth stating. Cells outside the retained window are absent from the data
entirely, so any statement this report makes about cellular heterogeneity concerns heterogeneity within
that window --- relevant to the second research question, since states of low transcript content, such
as quiescent or senescence-like cells, lie near its lower bound. And the 198 lines are where the sample
funnel begins: \NLines{} of them carry usable response measurements, and that number, not the size of
the drug screen, is what bounds the effective sample size, since the screen itself covers roughly
1{,}100 lines.

The source study additionally centers expression before its own analyses, in two forms, and neither is
adopted here. Centering each gene on its mean across all cells is already subsumed by the per-gene
standardization described below, which centers and scales. Centering each gene within each cell line is
a different operation: it removes between-line variation and retains only variation within a line.
Because the response label is constant across the cells of a line, all of its signal is between-line,
so per-line centering would remove precisely the variance the target depends upon. It is nevertheless
the natural representation for the second research question, and the point at which to revisit it is
the move to pooling over a line's cells rather than predicting from each cell separately.

\textbf{Expression preprocessing (revised 05.08.2026).} SCP542 is distributed as counts per million, so
library-size normalization has already been applied --- once, to the full gene matrix, which is where it
belongs. The pipeline therefore does not normalize a second time. Expression is transformed as
$E_{ij} = \log_{2}(1 + \mathrm{CPM}_{ij}/10)$, which is the quantification the dataset's own authors
use: they divide by ten because the average number of unique molecular identifiers detected per cell is
below $10^5$, and without it the difference between detected and undetected genes is inflated
\cite{scp542}. The \NHvg{} most variable genes are selected on that matrix using the dispersion-based
Seurat criterion; each gene is then standardized to zero mean and unit variance across cells, with
$z$-scores clipped at 10, before the principal components are taken
(\code{scripts/preprocessing/expression.py}, \code{scripts/preprocessing/scp542\_conversion.py},
\code{scripts/preprocessing/add\_pca.py}). Until this revision both steps used a plain $\log(1+x)$ on
raw CPM --- natural log, no divisor --- which has no justification specific to this data, whereas the
published choice is argued from a measured property of it.

The reason for transforming at all, rather than passing counts per million directly to the
decomposition, is that principal component analysis and every method built on Euclidean distance weigh
each gene by its absolute spread. On the untransformed scale a housekeeping gene at 5{,}000 counts per
million varying by a fifth separates two cells by a thousand units, while a transcription factor at
fifty counts per million varying by a factor of two separates them by fifty; the leading components
then report which cells carried more ribosomal and mitochondrial transcript, not which cells differ
biologically. Expression differences are multiplicative, and the logarithm converts a fold change into
a fixed additive distance wherever on the scale it occurs, so a doubling counts equally in a rare and
in an abundant gene. The per-gene standardization described next equalizes variance as well, but does
not substitute for the transform: gene selection precedes it and its dispersion statistic is defined on
the logarithmic scale, and standardizing a strongly right-skewed distribution still leaves a few
extreme cells dominating the gene.

Until this revision the PCA branch did two further things differently. It applied a second library-size
normalization \emph{after} gene selection, dividing each cell by the total of its retained genes alone,
which rescales a cell by the share of its expression that happens to fall inside the selected set and
scales the same cell differently in each gene-set variant. And it applied no per-gene standardization,
so the leading components tracked absolute expression level rather than variation between cells. Both
are corrected in the code; \textbf{every PCA number in this report was produced before the correction}
and will change when the representation is recomputed. The change of log transform may additionally
move which genes the variability criterion selects, since it alters the expression bins the dispersions
are normalized within, so the gene set itself is not guaranteed to be identical either.

\textbf{The two representations are preprocessed differently, and deliberately so.} scGPT receives the
same counts-per-million matrix with neither the logarithm nor the per-gene standardization applied. The
embedding entry point used here, \code{embed\_data}, exposes no normalization or transform argument at
all: the single operation applied to expression before the model reads it is a binning of each cell into
51 bins whose edges are quantiles of that same cell's non-zero values
(\code{scgpt/tasks/cell\_emb.py}, \code{scgpt/data\_collator.py}, \code{scgpt/preprocess.py} in the
vendored scGPT checkout; the call site is \code{scripts/preprocessing/gen\_embeds.py}). scGPT's separate
\code{Preprocessor} utility, which its fine-tuning tutorials use and which does normalize to a fixed
library size and select variable genes, is not on this path \cite{scgpt}.

Nothing is lost by omitting them, because binning is a rank transform. The bin edges are quantiles of the
cell's own values and each value is digitized against those same edges, so a strictly increasing map
applied within a cell --- dividing by the library size, taking a logarithm, or both --- moves the values
and the edges together and every gene lands in the bin it started in. Raw unique-molecular-identifier
counts, counts per million, and $\log(1+x)$ of either therefore present the model with identical input,
up to floating-point ties between values a transform renders equal, which the binning breaks at random
under the fixed seed. Normalizing before scGPT is not merely unnecessary; it is inert.

This is why the asymmetry is not an oversight. The principal-component baseline requires the logarithm
and the standardization because it measures Euclidean distance on the expression values themselves,
where each gene's influence scales with its absolute spread --- the argument given above. scGPT never
reads those values, only their order within each cell, so the same two operations would alter nothing it
sees. Applying them to both arms for the sake of symmetry would not make the comparison fairer; it would
only disguise the fact that the two representations consume different information from the same matrix.
It also means the choice of source matrix is settled for one arm and open for the other: the portal
additionally distributes the pre-quality-control unique-molecular-identifier counts, and substituting
them would leave the scGPT input bit-for-bit unchanged while changing gene selection and the principal
components.
\end{revisionblock}

A property of the data governs every error estimate in this report. The label is defined per cell line and
drug, not per cell, so all cells of a line share the same broadcast bulk value and act as
pseudo-replicates. The effective sample size is therefore the number of cell lines, approximately
\NLinesCV{} once the fixed test split is held out, not the \NCells{} cells. Treating cells as independent
would understate the uncertainty by roughly the square root of the number of cells per line.

\subsection{Drug panel selection}
\label{sec:panel}

The compounds a multi-task model predicts are a methodological choice, not a detail of setup, and the
choice made here is deliberately independent of the response values the model is trained on. Two earlier
panels in this project were selected using those values --- one by a differential-response gate requiring
a compound both to kill and to spare a population of cell lines, the other by ranking candidates on the
same quantity before a literature criterion was applied --- and both were discarded. The gate is
instructive because its defect is systematic rather than incidental: requiring absolute potency excludes
every cytostatic agent by construction, since a compound that arrests rather than kills never crosses the
viability threshold however sensitive the line, and a large share of targeted therapy is cytostatic. More
generally, selecting compounds on the spread of our own labels enriches for drugs that happen to separate
our particular cell lines, which flatters every correlation subsequently measured on them, and it requires
reading labels of cell lines that are later held out.

The panel is therefore defined by three conditions, of which only the second involves our data at all.
First, the compound must be approved by the FDA for a cancer indication, taken from the systematic
enumeration of Sun et al.\ \cite{sun_fda}, which lists 150 drugs approved between 1949 and 2014 together
with approval year, therapeutic class and target gene. Second, CTRPv2 must have screened it against at
least 90\% of the cell lines in the intersection with SCP542; this threshold is where the coverage
distribution breaks, and the break is sharp: 45 candidates lie at or above it, the lowest at 91.2\%,
while the next compound down reaches only 69.1\% and the tail falls to 28.7\%. Third,
a published claim about the compound's response determinants must exist and must be verified against the
source, recorded per drug together with what that source actually establishes. The first two conditions
are computed; the third is a literature judgement that cannot be, and is carried as a table whose entries
the code requires to lie inside the set the first two produce.

Applying these gives the following funnel. Of the 150 approved drugs, 120 have a small-molecule structure;
the remaining 30 are monoclonal antibodies, enzymes, a cell therapy and a radiopharmaceutical, which
cannot appear in a small-molecule viability screen. Of those 120, 57 were screened by CTRPv2, 45 clear the
coverage threshold, and 11 carry a verified published claim. The panel is \code{gemcitabine},
\code{doxorubicin}, \code{etoposide}, \code{paclitaxel} and \code{cisplatin} among cytotoxics, and
\code{imatinib}, \code{sorafenib}, \code{dasatinib}, \code{erlotinib}, \code{crizotinib} and
\code{afatinib} among targeted agents, with coverage between 91.2\% and 98.3\% of the available cell
lines.

Matching an external drug list to a screen is not a string operation, and treating it as one loses
compounds systematically. Approval lists name drugs by international nonproprietary name and often in
their salt form, whereas screens use development codes and free bases; furthermore the compound
annotation and the response values reach this project through two different files that name the same \NDrugs{}
compounds differently, so that a name-based join between them fails for 102 of the \NDrugs{}, of which 15 are
single-agent approved or clinical compounds. Four keys are therefore used and the successful one is
recorded per compound: the two name spaces, the PubChem compound identifier, and PubChem's
parent-compound relation \cite{pubchem}, which is what connects a salt named in an approval list to the
free base a screen dissolved. The last key alone accounts for 13 of the matches, among them imatinib and
doxorubicin. The join between the two internal files is made on the screen's own compound identifier
rather than on names, which matches all \NDrugs{}. One compound required an explicit correction: cisplatin is
recorded under a nonstandard name and carries a structure identifier pointing to elemental platinum
rather than to the drug, and is resolved here on the screen's own recorded structure and vendor
catalogue entry.

Platinum agents are represented once rather than three times, although cisplatin, carboplatin and
oxaliplatin are all screened with near-identical coverage. Cisplatin and carboplatin share the
\emph{cis}-diammine carrier ligand and form the same 1,2-intrastrand adduct; they are largely
cross-resistant and are governed by the same repair biology, so including both would spend two positions
in an eleven-drug panel on one resistance mechanism. Oxaliplatin's diaminocyclohexane adduct evades
mismatch repair and is genuinely complementary, but its expression-level determinant is the least
established of the three. Cisplatin was selected as the representative of the class.

One entry is reported as contested rather than as supporting evidence, because the panel should not
appear better grounded than it is. The compound in question is cisplatin, which is itself named in two
of the four papers underlying this work --- it is one of the five drugs the single-cell benchmark of
Chen et al.\ \cite{scdeal} is built on, and a perturbation tested against expression heterogeneity by
Kinker et al.\ \cite{scp542} --- so what is contested is its response determinant rather than its
standing in the relevant literature. The standard determinant of platinum resistance is ERCC1, and the two
directly comparable cell-line studies disagree: ERCC1 mRNA correlates with cisplatin resistance in
cervical carcinoma lines \cite{britten_ercc1}, while no association is found across twenty lung cancer
lines for cisplatin or carboplatin \cite{shimizu_ercc1}. Neither supersedes the other, as they differ in
lineage and assay, and the more recent is also the less cited. The likely explanation is that ERCC1 has
four splice isoforms of which only one performs DNA repair, and available assays cannot distinguish them
\cite{friboulet_ercc1}. This bears directly on the present work: the features used here are gene-level
expression, in which the functional isoform is not representable, so poor performance on cisplatin is
anticipated by the literature rather than diagnostic of the model. Retaining the compound with its
determinant marked contested is preferred to restricting the panel to drugs whose determinants are
settled, which would bias the evaluation toward compounds whose answer is already known. By the same
reasoning the evidence supporting doxorubicin and etoposide, both substrates of the ABCB1 efflux
transporter \cite{skinner_abcb1,omori_abcb1}, is recorded with its setting, and topoisomerase expression
is not claimed as a determinant for either, since it does not correlate with sensitivity where it has
been measured \cite{omori_abcb1}.

Finally, the availability of a fitted IC50 is deliberately excluded from the selection criteria. An IC50
is undefined when a dose-response curve does not cross half-maximal viability, so the proportion of cell
lines for which one exists is a measure of a compound's potency, ranging within this panel from 14 to 174
of the available lines. Selecting on it would reintroduce the potency filter described above under the
appearance of a data-quality rule. Where the two response measures are compared, the comparison is
restricted at evaluation to compounds with sufficient IC50 coverage, with the per-compound counts
reported. The selection is implemented in
\code{notebooks/2\_drug\_selection.ipynb}, which reads only the response table and writes
the panel with its citations to \code{notebooks/outputs/panel/panel.csv}; the compound identifier
resolution is in \code{scripts/annotation/drug\_annotation.py} and
\code{scripts/sources/pubchem.py}.

\subsection{Representation and model}

The comparison between representations is controlled so that only the content of the embedding differs.
Both the PCA baseline (\code{X\_pca}) and the scGPT embedding (\code{X\_scGPT}) are 512-dimensional and are
fed into the same multi-head perceptron, with hidden dimensions 128 and 64, layer normalization, GELU
activations and dropout. The network has one output head per drug and is trained with a masked
mean-squared-error loss that is evaluated only on observed cell-drug pairs. The model and training code are
defined in \code{scripts/model/OncoMLP.py} and \code{scripts/training/train\_multitask.py}.

\begin{revisionblock}
\textbf{Splits, and where each representation is fitted (added 05.08.2026).} Train, validation and test
are a 70/15/15 partition of cell lines, so every cell of a line falls on the same side and no line is
split across parts. The assignment is \emph{frozen to a versioned file}
(\code{splits/split\_ctrp.csv}) instead of being redrawn from the data on each run. It has to be:
eligibility depends on which compounds survive the label filters, so a change to the drug panel would
otherwise move lines between train, validation and test unannounced, and results from either side of
that change would look comparable while being scored on different held-out lines. Redrawing requires an
explicit flag, and a line that the frozen file does not cover is an error rather than a fresh draw.
\revision{\emph{Correction, 12.08.2026.} The mechanism is as described, but the file itself had never
been written to the repository, so on every run to date the assignment was in fact redrawn and the guard
never applied. It is created when the pipeline is next run and versioned from that point. No result is
affected: the partition is reproducible from the stored data, and the set of lines it placed in training
and validation is recorded in the committed out-of-fold predictions.}

The principal components used as model input are fitted on the training lines of that split alone ---
per-gene standardization and the rotation both --- and then applied to the held-out lines
(\code{X\_pca\_train\_ctrp}). A separate all-cells decomposition (\code{X\_pca}) is kept for description
only, such as the latent-space figures, where holding cells out would be wrong. The cross-validation
results are the exception: its folds are drawn at training time rather than stored, so no precomputed
train-only projection exists for them and they still use the all-cells decomposition. scGPT needs no
such distinction --- its weights are pretrained and frozen, and its per-cell binning uses only the cell
being embedded, so nothing about it is fitted on this data.
\end{revisionblock}

\begin{revisionblock}
\textbf{What a fit is allowed to see --- decided 12.08.2026.} Three quantities in the pipeline are
estimated from data before any model is trained: the selection of variable genes, the per-gene
standardization, and the principal component rotation. None of them sees a response label, which is why
conventional pipelines accept them without comment, and it is also why they were settled together rather
than one at a time: they are the same question asked three times, and separate answers would have been
inconsistent. \textbf{This paragraph records the decisions, not the state of the code}: one of the three
changes what the pipeline does and takes effect when the representations are regenerated, so the
description immediately above --- in which the cross-validated results use the all-cells decomposition
--- remains accurate until then, and is deliberately left standing rather than rewritten in advance.

\textbf{Gene selection continues to use every cell.} The alternative --- choosing variable genes within
each fold's training lines --- makes the gene set fold-dependent, so that at five folds there would be
five gene sets and the phrase ``the five thousand most variable genes'' would no longer name a single
object. Comparisons across folds, between the two representations, and across the gene-set sweep would
then differ in which genes they contain as well as in which cells, and the sweep in particular is defined
by holding the gene count fixed. Comparability is judged worth more than removing an unsupervised fit.
The cells belonging to lines that carry no response measurement, about a ninth of the atlas, are retained
in that selection for the same reason: they cannot leak a label they do not have, more cells estimate a
gene's variance across cells more precisely, and both representations receive the identical set, so
neither is advantaged.

\textbf{The rotation used by the cross-validated results will be fitted per fold} --- the one decision of
the three that changes the pipeline, and the one that matters most, because every cross-validated figure
carried the all-cells rotation and the cross-validated figures are the headline. \textbf{Its fitting set
is the cells the model's own weights are fitted on}: the fold's training side \emph{less} the fifteen per
cent of lines withheld for early stopping, rather than the whole training side. The stricter choice is
deliberate and is argued below. Storing five fold-specific matrices was considered and rejected: it would
cost roughly a gigabyte across the two gene-set variants and, more importantly, would push the
cross-validation's configuration into the preprocessing stage, so that changing how folds are drawn would
require rebuilding the stored representations. The fits are instead cached per run and keyed by the fold
assignment, which is reproducible from the seed and the eligible lines. The all-cells decomposition is
still written and remains correct for descriptive use such as the latent-space figures; both it and the
per-fold fits are computed in single precision, so that the only thing distinguishing them is which cells
they see.

\textbf{What each representation actually receives, since the difference is narrower than it is usually
stated.} It is tempting to say the embedding is not fitted on this data at all, and that is wrong: it is
fed the same counts-per-million matrix as the baseline. Setting the two side by side, the operations
before the model are these. Library-size normalization divides each cell by its own total, and the
logarithm is elementwise, so neither is estimated across cells and both arms receive them identically.
Gene selection \emph{is} estimated across cells, and both arms receive its result --- one shared gene set.
The embedding then bins each cell's values against quantiles of that same cell's non-zero entries, which
involves no other cell, and passes them through weights pretrained elsewhere and frozen here. The
baseline instead estimates a per-gene mean and standard deviation and a rotation, and those require a
fitting set.

That last row is the \emph{only} asymmetry. Same cells, same normalization, same transform, same genes
--- and one fit that the baseline needs and the embedding does not.

\textbf{Which is precisely the argument for the stricter fitting set.} Because that fit is the single
place the two arms differ, how much it is allowed to see is not a detail of setup: it is the one knob
that could move a measured difference for a reason having nothing to do with the representations. Fitting
on the whole training side would hand the baseline roughly eighteen per cent more cells than the model
that consumes it was trained on, and those are cells the embedding structurally cannot benefit from.
Restricting the fit to the model's own fitting cells costs about fifteen per cent of the cells available
to a 512-component decomposition, which is a real cost and is accepted for what it buys: a difference
between the arms that is attributable to the representation rather than to the generosity of the
control's fit.

\textbf{What remains, stated carefully, because the restriction changes the argument rather than merely
strengthening it.} Before this decision the baseline's fit was estimated over every cell, including
held-out ones, and that was the ground on which any advantage measured for the embedding was called
conservative. Restricting the fit to the model's own fitting cells largely removes that ground: the
baseline is no longer seeing more than the model that consumes it. Gene selection is still estimated
over all cells, but both arms receive the identical set from it, so it does not favour either. The
conservatism claim therefore does not carry over unchanged, and is not restated here on the old
reasoning.

What does survive is a different asymmetry, described earlier in this section and not repeated here: the
baseline decomposes every selected gene, while the embedding reads only those genes present in its
vocabulary. That one is unaffected by which cells a fit sees, and it handicaps the same arm. The
restriction's purpose is narrower than conservatism and is worth stating as what it is --- with the fit
no longer the place the arms differ in how much they see, a measured difference becomes attributable to
the representations themselves, which is the only thing this comparison was ever able to claim.
\end{revisionblock}

\begin{revisionblock}
\textbf{Early stopping is nested inside the training lines (added 12.08.2026).} Training runs for a
fixed maximum number of epochs and the checkpoint with the lowest validation error is restored, so the
stopping epoch is a quantity chosen from data. Until 12.08.2026 the cross-validation harness measured
that error on the fold it was about to score, which makes every reported figure a minimum over up to
twenty-five epochs on its own evaluation data. Each fold now withholds fifteen per cent of its training
cell lines, grouped as everywhere else, as the early-stopping set
(\code{scripts/training/cv.py::inner\_holdout}), so the scored fold decides nothing about the model that
predicts it; the per-drug-mean baseline and the head initialisation are fitted on the remaining fitting
lines for the same reason. This is the arrangement the DrEval benchmark itself uses, whose per-fold
splits are three-way. The fifteen per cent is arbitrary --- it was preferred to reusing a whole
neighbouring fold, which would have cost twenty per cent of the training lines.
\end{revisionblock}

\subsection{Response target}
\label{sec:target}

The target is \code{auc\_cc}, the area under the fitted dose-response curve as re-fitted by DrEval
\revision{\textbf{A note on the per-curve confidence intervals, 14.08.2026.} Their
\code{\_low}/\code{\_high} labels do not order by numeric value: \code{p1\_conf\_int\_low} exceeds
\code{p1\_conf\_int\_high} in every populated row. That is a naming convention rather than an error ---
the bounds refer to the opposite end of the dose axis, which inverts once the parameter is expressed
in the fitted units --- and the intervals are sound: the corresponding point estimate lies inside
$[\,\text{high},\text{low}\,]$ in $100.0\,\%$ of the 392{,}648 rows for the first parameter and
$99.2\,\%$ for the fourth. Recorded because reading them in the obvious order gives an empty interval.
This work does not use them; the fit statistics it relies on are \code{R2} and \code{pValue}.}
\cite{dreval} from CTRPv2's raw measurements. Their reprocessing normalizes each replicate against its
own no-drug control and fits one curve with CurveCurator, folding replicate disagreement into the fit
rather than averaging replicates before fitting --- which their Methods argue is the practice that
avoids misleading response measures where replicates diverge sharply. A \emph{higher} value means a
\emph{less} sensitive cell line, and 1.0 is the no-effect level, because the fit pins the
low-concentration asymptote to the vehicle control \cite{ctrpv2}.

The same fit yields a second measure, \code{ln\_ic50\_cc}, the natural log of the IC50. It is not the
default and is used only where the two are compared, because it is undefined for roughly 40\% of curves
by construction: an IC50 falling well outside the measured dose range is discarded, which is the case
for most compounds that arrest rather than kill. Selecting on its availability would therefore be
selecting on potency. Each measure is written to its own targets file, so the two can be compared
without either overwriting the other. Implemented in
\code{scripts/preprocessing/ctrp\_to\_h5ad.py}; the source is pinned to one Zenodo record by
\code{scripts/layout.py}.

No winsorization and no quality filter are applied. The response is used as the source publishes it:
the benchmark this work follows applies no clipping either, in order to stay comparable with earlier
studies and to avoid discarding data.

\revision{\emph{Two earlier targets are not described here, deliberately (12.08.2026).} This subsection
previously defined \code{auc\_z}, a per-drug standardized target used from 13.07.2026 to 27.07.2026,
and argued for it at length. Both it and the \code{auc} measure it standardized have been removed from
the pipeline, and describing a retired target in Methods invites it to be read as part of the current
one. What survived the retirement is not a target at all but a finding about the objective --- that a
shared multi-task loss can be dominated by a few high-variance compounds --- and that is stated in
Section~\ref{sec:discussion}, where it belongs. The full decomposition, the leak it closed, and the
superseded numbers are in \code{docs/steps/03-model-and-training-design.md} and
\code{docs/steps/corrections-and-dead-ends.md}.}

A minority of cell-line--drug combinations were screened by CTRPv2 in two separate experiments. Their
two measurements are averaged into the single value used as the target, without weighting by replicate
count and without excluding pairs whose measurements disagree; where a combination was screened once,
that measurement is used as is.
\begin{revisionblock}
\textbf{The pipeline no longer averages anything, and has not since 11.08.2026.} The sentence above
describes the previous target build and is retained only because the measurement that follows it was
made against that build. Under the current source, replicate disagreement is handled inside the curve
fit rather than after it: DrEval fit one curve across a compound's replicates, so a repeated
measurement arrives already reconciled. What reaches the pipeline is one fitted value repeated once
per source experiment, and \code{ctrp\_to\_h5ad.\_deduplicate\_measurements} drops those exact
duplicates and \emph{raises} on any pair that is not identical, rather than reconciling it. The
averaging rule this paragraph states could therefore not run today; a disagreeing pair is an error
someone has to decide about, which is what makes the decision revisitable.

The numbers below are unaffected as measurements but are stale as description. They were computed on
CTRPv2's own 2015 distribution, which stopped being the target source on the same date, so they
quantify the problem that motivated the switch rather than a property of the target now in use. They
are due for re-derivation on the current source, and the notebook that produced them is archived
because it reads tables the pipeline no longer touches.
\end{revisionblock}
The disagreement between repeated measurements is nonetheless
substantial and is reported here rather than left implicit, because it sets the scale against which any
correlation in Section~\ref{sec:results} should be read. Of the cell-line--drug pairs inside the
SCP542 intersection, \NRepPairs{} were screened twice, never more. Expressed relative to the spread of
that drug's response across cell lines --- which is precisely the quantity the model is asked to
predict --- the two measurements differ by a median of \RepDiffMed{} of one standard deviation, and
\RepDiffPct{}\% of repeated pairs differ by more than a full standard deviation. The median is used in
preference to the mean because a small number of extreme pairs would inflate the latter; it is the
middle pair, so half of the repeated measurements disagree by less than \RepDiffMed{} standard
deviations and half by more. A caveat limits how
far this generalizes: the repeats come from only \NRepLines{} cell lines, each re-screened against
almost the whole compound set, and CTRPv2 did not select those lines at random. The figure therefore
indicates that a material share of the target is screening noise, but it does not establish a numeric
ceiling on the achievable correlation. Quantified in
\code{notebooks/archive/replicate\_variation.ipynb}, with the underlying pairs in
\code{notebooks/outputs/archive/replicate\_variation.csv}.
\revision{\textbf{Closed 14.08.2026 --- the re-derivation flagged above will not be performed, and
does not need to be.} The labels are taken from the reprocessed table rather than
computed here, and that table carries one curve per (cell line, compound) with replicate variability
folded into the fit, so no pair of numbers remains in it to disagree. Recomputing the labels from
CTRPv2's raw well-level measurements is possible in principle --- those files are held, and fitting
each replicate separately would restore the pair --- but that is a re-derivation of the target itself
rather than a diagnostic, and it would replace every label in this work with one produced here. It
would also be of narrow reach --- only 19 of CTRPv2's 887 cell lines were screened more than once, of
which \NRepLines{} lie inside this project's \NLines{}, or $3.3\,\%$. Label quality is instead measured
directly from the per-curve fit statistics that accompany every label, which cover all 1{,}971 curves
behind this panel; see Section~\ref{sec:results}. The figures above are retained as the record of what
was measured on the previous source.}

\subsection{Evaluation}

Cell lines are evaluated out of fold. A 5-fold group $k$-fold split, grouped by cell line, is applied to
the 153 training and validation lines, so that no cell of a test line is seen during training. Each line
is predicted exactly once, the per-cell predictions of a line are averaged to the line level, and the
Spearman correlation between predicted and true values across those lines is computed per drug and
averaged over drugs. This protocol replaces an earlier fixed 27-line validation split, on which the
standard error of a correlation is approximately 0.2 and effects below 0.4 are not measurable --- a
property of the sample size, not a measurement on these data.
\revision{\emph{One sentence was removed here (12.08.2026): that the Pearson correlation tracks the
Spearman value within 0.02 throughout.} It is an empirical claim about runs on the retired target and the
voided panel, and nothing has been re-run, so the agreement between the two coefficients is unmeasured on
the current pipeline. Both are reported when they are re-measured; neither may stand in for the other
until then.
\emph{A macro was also split on the same date.} The effective sample size was cited through a single
macro, \code{NIndep}, defined as ``independent examples after held-out splits'' but carrying the value
150 --- which was the line count of a ridge control, a different quantity, and one this report no longer
reports. Since one name stood for two numbers it was retired, for \code{NLinesCV} (\NLinesCV, the lines
inside the cross-validation, test held out) and \code{NLinesFit} (\NLinesFit, the fixed training split
alone). Every site citing the effective sample size takes the former. The paragraph above pins it: it
names the \NLinesCV{} training and validation lines, and the sentence after cites the effective sample
size for the same protocol, so the two cannot differ. That also matches what the quantity is for --- each
use of it says that uncertainty is governed by cell lines rather than by cells, and the metric reported
is a correlation across the lines the cross-validation predicts exactly once. The fixed training split is
a training-set size, and no uncertainty reported here is taken over it. The plural in ``held-out splits''
was loose: under this protocol there is no standing validation holdout at all, since the early-stopping
slice is drawn from each fold's own training lines.}

For external comparison, the model is additionally evaluated with the DrEval package \cite{dreval}
(version 1.5.1), using its leave-cell-line-out splits, its baselines and its evaluation routine without
reimplementation. As recommended by the framework, metrics are reported after the mean drug and cell-line
effects have been removed from both the true and predicted responses, and they are pooled over all
cell-line-drug pairs rather than averaged per drug, so they are not directly comparable to the per-drug
correlations above.
\begin{revisionblock}
\textbf{Under this split design the cell-line effect is not in fact removed (added 12.08.2026).} The
sentence above describes the normalization as the framework defines it, and the implementation is
theirs unmodified --- but stating it that way overclaims what the metric controls for here, and
Section~\ref{sec:discussion} already says so. The baseline subtracted is
\code{overall mean + cell-line effect + drug effect}, fitted on the training folds. Under
leave-cell-line-out every scored line is by construction absent from those folds, so its effect
resolves to zero and the subtraction removes the \emph{drug} effect alone.

This is correct behaviour for a prediction benchmark --- no model can know an unseen line's effect ---
but it means a high normalized score is not evidence that a model has learned anything drug-specific: a
predictor emitting nothing but \code{mean + line effect + drug effect}, with no drug-specific signal
whatsoever, retains the line effect in truth and prediction alike, where it correlates with itself. The
question of how much of a per-drug correlation is drug-specific biology rather than general cell-line
fragility is therefore left open by this metric, and answering it requires a diagnostic that reads
held-out labels --- which is not a metric and is deferred rather than folded into the benchmark.
\revision{\emph{A figure was removed from this paragraph (12.08.2026).} It reported that such a synthetic
predictor scores a normalized Spearman correlation of 0.98 under this protocol, and presented it as
demonstrated rather than argued. No code in this repository produces that value and no stored artifact
records it: it appears only in prose, in this section and in the module docstring it was taken from.
Under this project's own convention a number with no re-runnable producer is not a result, so it is
withdrawn. What survives without it is the mechanism, which is verifiable from the source --- an unseen
cell line receives effect zero, which is \code{drevalpy}'s behaviour and not a choice made here, so the
subtraction removes the drug effect alone. Quantifying the consequence needs a synthetic-predictor check
that is written down and run.}
\end{revisionblock}

\subsection{Multiple-instance learning and the heterogeneity criterion}

Nothing in this subsection has been run. It records an architecture and a decision rule fixed on
13.08.2026, before the corresponding model existed, and it appears in Methods rather than in Results for
exactly that reason: a criterion chosen after the numbers are visible is not a criterion. The
implementation is \code{notebooks/4b\_mil\_training.ipynb}, whose section 2 is the authoritative
statement of what follows.

\textbf{Why the per-cell model cannot answer the heterogeneity question.} Every cell of a line carries
that line's bulk label, so the per-cell objective asks each of a line's cells for the same number. This
is not merely unhelpful for a question about within-line variation; it penalizes it, and the penalty is
exact. For one drug and one line with per-cell predictions $p_c$, mean prediction $\bar{p}$ and the
line's single label $y$,
\begin{equation}
  \frac{1}{n}\sum_{c=1}^{n}\left(p_c - y\right)^2
  \;=\; \underbrace{\left(\bar{p} - y\right)^2}_{\text{bag loss}}
  \;+\; \underbrace{\mathrm{Var}_c(p)}_{\text{penalty on within-line spread}},
  \label{eq:mil-decomposition}
\end{equation}
an identity rather than an approximation, since $\sum_c (p_c - \bar{p}) = 0$ removes the cross term. The
left-hand side is what the per-cell model minimizes, regrouped by line; the first term on the right is
what a mean-pooled multiple-instance model minimizes. The two objectives differ by precisely the
within-line variance of the predictions, at full weight, in every batch. The regrouping is an analysis
and not something the code performs --- batches are cells drawn across lines --- but it is valid because
the loss is a sum over cells and cells partition into lines, and the density weighting does not disturb
it, being a function of the label and therefore constant within a line. It follows that the heterogeneity
question is unanswerable under the per-cell architecture as a matter of arithmetic, not of insufficient
capacity: no hyperparameter reaches this term, and only removing it does.

\textbf{Instance-level rather than attention pooling.} Of the two standard formulations
\cite{ilse2018}, the embedding-level variant pools a bag's instances and predicts once, while the
instance-level variant gives every instance its own prediction and aggregates those. Embedding-level
generally predicts better. It is nonetheless rejected here, because the question asked is whether
\emph{individual cells} receive different predicted responses, and only the instance-level formulation
produces a quantity at that resolution. The expected cost in predictive performance is accepted
deliberately.

\textbf{One bag is one cell line, and bags are complete.} The bag follows from the label rather than
being chosen: the response is measured once per (cell line, drug), so a line's cells are the natural bag
and the multi-head output makes one forward pass yield every drug at once. Bags are \emph{complete} ---
every sequenced cell of the line, not a fixed-size sample. This too is forced rather than convenient. For
a sub-bag of $B$ cells drawn from a line of $n$, the expected loss is
$(\bar{p}_n - y)^2 + (\sigma^2/B)\,(1 - (B-1)/(n-1))$, which at $B = 1$ is exactly the per-cell loss and
at $B = n$ contains no variance term at all. Bag size therefore interpolates continuously between the two
architectures and sets how strongly the objective charges for the very quantity under study; it is not a
batching parameter. Complete bags also preserve the property that one line contributes one training
example, which is what removes the per-cell objective's implicit weighting of lines by sequencing depth.
The costs are one gradient step per line and memory scaling with the largest line.

\textbf{Mean pooling, fixed in advance and not revisited.} The bag prediction is the mean of its cells'
predictions, matching the definition of the label it is compared against. Two alternatives were
considered and rejected before any run: adding a variance term to the loss, which would make the
spread criterion below pass by construction and would change the objective at the same time as the
architecture; and a maximum or upper-quantile aggregator, which does not force variation either --- the
maximum of equal values is that value --- and mismatches an averaged label. Mean pooling carries a
shrinkage incentive, in that collapsing a bag's cells onto their mean is a minimizer whenever the model
cannot do better, and that incentive is why the criterion below leads with a positive control. If the
control fails, the aggregator is \emph{not} exchanged for one that passes: adjusting the instrument until
it certifies itself is a forking path moved down one level, and it would leave any subsequent positive
result unattributable.

\textbf{The criterion, and why it contains no chosen thresholds.} Five stages are fixed in advance. An
\emph{input ceiling} measures the within-line share of total variance in each representation before any
training, since no model can separate cells that the representation places at one point. A
\emph{positive control} builds bags by mixing two cell lines of known, deliberately similar response and
asks whether the model ranks the two source populations apart \emph{within} a single bag. A \emph{spread}
stage requires the within-line standard deviation of per-cell predictions to exceed that of the per-cell
model. A \emph{reproducibility} stage asks whether independent seeds assign high and low predictions to
the same cells. A \emph{confound} stage regresses the predictions on total counts, genes detected,
mitochondrial fraction and cell-cycle score, and can veto a pass that the preceding stages granted.

\textbf{Two of the veto's covariates are recovered from the raw counts.} The expression matrix reaches
this project as counts per million, so the library size each cell was sequenced to has been divided
out of every processed file, and only four of the thirteen mitochondrial genes survive selection of
the five thousand most variable ones. Neither sequencing depth nor mitochondrial fraction is therefore
recoverable from the data as the model sees them --- a fact established after the criterion had been
fixed, which left the veto briefly unevaluable. Both are recovered instead from the study's own
unnormalised UMI matrix, read once and joined to the cells by barcode, so the stage is evaluated as
specified rather than narrowed to the covariates that happened to be available.

Both are computed over the complete gene set, and the reason is not precision. A total restricted to
the highly variable genes is not depth: it is depth multiplied by the share of a cell's counts falling
in that set, and that share is itself biological, since a cell expressing the variable programs
strongly has a larger one by construction. A confounder carrying the signal under test can veto a true
positive, which is a worse failure than a noisy confounder. The same argument applies to the
mitochondrial fraction, where restriction would leave a truncated numerator over a denominator missing
most of the transcriptome. What the veto still requires, and what no null supplies, is the magnitude
at which the confounders count as explaining the variation: with hundreds of cells in a line, a
proportion of variance far too small to matter is nonetheless significant against a permutation null.
That magnitude is reported rather than fixed in advance.

The quantity it will be read on, however, is settled: the proportion of within-line variance explained,
\emph{adjusted} for the number of regressors. The unadjusted proportion is biased upward by the count of
covariates, and that bias falls as the number of cells rises, while a cell line here contributes between
fifty-six and one thousand nine hundred and ninety cells. A single threshold on the unadjusted figure
would therefore be a different threshold for every line --- a small line clearing a bar that a large one
fails on identical evidence --- which is precisely the axis the comparison runs along. The adjusted
figure removes that, at the cost of being able to fall below zero when the covariates explain nothing,
which is a correct reading rather than a defect. The difference is not cosmetic: on simulated
predictions bearing no relationship to the covariates at all, the unadjusted proportion reads about
nineteen per cent and the adjusted one about minus one.

An earlier draft attached invented magnitudes to three of these. Each was removed by replacing the bar
with a null or a comparison rather than by choosing a better number. The input stage now fails only on
collapse and otherwise reports its ratio, collapse being the precondition it actually stands for. The
control's separation is measured as an area under the receiver-operating characteristic --- nothing is
classified; this is the Mann--Whitney rank statistic divided by $n_A n_B$ \cite{mannwhitney1947} --- and
it passes by beating the null obtained from permuting the source labels within the bag, which is that
statistic's own null. The area itself is reported as the instrument's measured sensitivity, and any
negative result downstream is stated with it attached. The spread stage compares against the per-cell
model with no margin, because equation~(\ref{eq:mil-decomposition}) shows the comparison to be a test of
precisely the deleted term. The reproducibility stage passes by beating a null in which cell identities
are permuted within each line. What remains chosen rather than derived is the definition of a
``similar'' line pair for the control, taken as the lowest quartile of a drug's pairwise response
differences.

The control is scored inside the bag rather than on it, and the distinction is load-bearing. Because the
bag prediction is a mean, a model that assigns every cell in a bag the same value can reproduce the bag
label exactly while doing none of what the remaining stages measure. A bag-level control would therefore
certify a collapsed instrument. Bag-level recovery is still computed across mixture weights and reported,
but it cannot pass the stage.

\textbf{What the criterion can and cannot establish.} Three questions have to be kept apart. Whether
per-cell predictions vary within a line, reproducibly and not as a sequencing artifact, is what the five
stages address. Whether that variation reflects real cellular heterogeneity of drug response, and whether
it identifies the cells that survive treatment, are not addressed and cannot be, because no per-cell
response was ever measured: every label in these data is one number per cell line and drug, and none of
the primary sources carries post-treatment single-cell profiles. This is a limitation of the available
measurements rather than of the design, and no threshold or architecture closes it. The first question is
nonetheless a necessary condition for the second, so a model that fails it has definitively not learned
heterogeneity, and a model that passes has earned a hypothesis rather than a finding.
