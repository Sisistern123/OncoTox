# Data and large artifacts (paths outside repo may still use Desktop/OncoTox/data)
data/

# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
.venv/
venv/
env/

# Packaging / build
*.egg-info/
dist/
build/
*.egg

# Jupyter
.ipynb_checkpoints/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE and OS
.idea/
.vscode/
*.swp
*~
.DS_Store

# AnnData / embedding outputs (if written under the repo tree)
*.h5ad

# Training run artifacts (checkpoints, configs, histories, per-drug results).
# The runs_index.csv ledger is committed by hand if you ever want to share it.
runs/
